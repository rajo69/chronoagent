# ChronoAgent: Session Context for Claude Code

> This file is auto-read at every session start. Keep it current. Full plan is in PLAN.md.

---

## Current Status

| Field | Value |
|-------|-------|
| **Current phase** | Phase 10: Research Experiment Suite |
| **Next task** | **Phase 10 task 10.8: analysis tables.** Task 10.7 is shipped locally on branch `feat/phase10-task10.7-analysis-plots` (pending commit at session end); push and open a PR against `main` mirroring the 10.6 PR #35 format (cite 1429 passed / 96.06% coverage / ruff + ruff format --check clean on 128 files / mypy --strict --no-incremental clean on 79 src files; 1 git mv (analysis.py -> analysis/phase1.py) + 2 new src files (analysis/__init__.py, analysis/plots.py) + 1 new test file with 41 tests; runner extension adds `RawRunRecord` + `collect_raw=True` flag + raw_runs persistence). After CI goes green on 3.11 + 3.12, merge with `gh pr merge --squash --delete-branch`, sync local `main`, THEN cut `feat/phase10-task10.8-analysis-tables` from the updated main. **Task 10.8 scope per PLAN.md:** `experiments/analysis/tables.py` with LaTeX table generators consumed by the paper. Three tables required: (1) **Main results table** -- one row per experiment (main_experiment, agentpoison_experiment, baseline_sentinel, baseline_no_monitoring) with columns for AWT mean +/- 95% CI, AUROC mean +/- 95% CI, F1 mean +/- 95% CI, and allocation efficiency mean +/- 95% CI. Reads `<results>/<name>/aggregate.json` for each experiment. (2) **Ablation table** -- one row per ablation (full system, no_forecaster, no_bocpd, no_health_scores) with the same four metric columns; the deltas vs full system go in parentheses. Same data source. (3) **Signal validation table** -- one row per signal column (the 6 columns from `chronoagent.monitor.collector.SIGNAL_LABELS`) with Cohen's d and large-effect flag from a Phase 1 `analysis.phase1.SignalAnalyzer` run, OR if 10.8 chooses not to re-run Phase 1, citing the GO/NO-GO ruling from `docs/phase1_decision.md`. **Key considerations:** (a) Use plain LaTeX tabular output, no booktabs unless already in pyproject. (b) Format `mean +/- ci` cells with consistent decimal places (3 decimals for AUROC/F1/AE, 1 decimal for AWT since it is in step units). (c) Format NaN means as "--" so a non-detected NoMonitoring row reads cleanly. (d) Bold the best value per column in the main results table so readers can scan. (e) Each table function takes a `results_dir: Path` and a list of experiment names + writes to `<results_dir>/<name>/tables/<table_name>.tex` for per-experiment context, OR `<results_dir>/tables/<table_name>.tex` for the multi-experiment tables. (f) Suggested test file: `tests/unit/test_experiment_tables.py` that uses the same fake-runner fixtures as `test_experiment_plots.py`, asserts the LaTeX files are written, parses them back to confirm row count + column count, and pins the per-cell formatting on a hand-crafted aggregate. **NOTE:** 10.8 does NOT touch `src/chronoagent/cli.py`; task 10.9 is the CLI wiring and happens after 10.8. The plot module's `_EXPERIMENT_DISPLAY_NAMES` and `_EXPERIMENT_COLOURS` tables already exist and can be reused / shared via a small helper module if convenient. |
| **Blocked?** | No |
| **Last session** | 2026-04-11. **Phase 10 task 10.6 merged to `main` via PR #35 squash commit `5ccd655` (all 4 CI checks green on 3.11 + 3.12). Phase 10 task 10.7 shipped on branch `feat/phase10-task10.7-analysis-plots`, PR to open against `main`.** Local pre-push run for 10.7: ruff check + format --check clean on 128 files, mypy --strict --no-incremental clean on 79 source files, pytest 1429 passed (1388 prior + 41 new plot tests) 96.06% coverage, zero flakes this run. **What 10.7 shipped:** New `src/chronoagent/experiments/analysis/` subpackage (converted from the single-file `analysis.py` via `git mv` so Phase 1 history is preserved). `analysis/__init__.py` re-exports `AnalysisConfig` and `SignalAnalyzer` from the relocated `analysis/phase1.py` so the existing CLI import path `from chronoagent.experiments.analysis import AnalysisConfig, SignalAnalyzer` keeps working unchanged. New `analysis/plots.py` (~640 lines) ships six paper figure functions plus an `ExperimentArtefacts` loader and a `generate_all_plots` convenience entry point. Six figures, one per Section 5 subsection: `plot_signal_drift` (5.1, time-series of every signal column for one representative run with injection + first-flagged steps marked, reads `raw/run_<i>_signals.npy`), `plot_health_score_comparison` (5.2, mean per-step detector score across runs for full system + ablations on the same axes with shaded ±1 std band, reads `raw/run_<i>_decisions.json`), `plot_awt_box` (5.3, box plot of per-run AWT values across baselines + full system, reads only `runs.csv` so it works without raw data, draws hatched grey placeholder boxes for empty AWT columns like NoMonitoring), `plot_allocation_efficiency_over_time` (5.4, per-step cumulative success rate averaged across runs, reads decisions JSON), `plot_roc_curve` (5.5, per-experiment ROC curves built from per-step pooled scores via `_empirical_roc` helper plus `detection_auroc` from 10.2 so the AUC label and the aggregate.json metric agree), `plot_ablation_bar_chart` (5.6, four-metric grouped bar chart with 95% CI error bars from `aggregate.json` only). Each figure writes both PNG and SVG. Per-experiment figures land under `<results>/<experiment>/figures/`; multi-experiment comparison figures land under `<results>/figures/`. Pinned `_EXPERIMENT_COLOURS` and `_EXPERIMENT_DISPLAY_NAMES` tables give the figures a consistent visual identity across regenerations. **10.6 runner extension landed in this task** (necessary because the plot module needs per-step raw data the 10.6 aggregate.json does not carry): `RawRunRecord(run_index, seed, signal_matrix, decisions)` frozen dataclass; `collect_raw: bool = False` constructor flag on `ExperimentRunner` (default off so existing 10.6 tests pay zero cost); `raw_runs` property exposing the live list; `_project_decision_for_raw` helper that turns Sentinel / NoMonitoring / FullSystem decisions into a JSON-friendly dict with stable schema (default `score=0.0`/`flagged=False` for NoMonitoring, only emits `bocpd_score`/`forecaster_score`/`integrity_score` when the source decision had them set); optional `raw_runs` parameter on `write_experiment_results` that writes `<results>/<name>/raw/run_<i>_signals.npy` (np.save) plus `run_<i>_decisions.json` (strict JSON via the existing `_nan_to_none` walker). **Key design decisions locked in:** (1) **Subpackage refactor uses `git mv` to preserve history.** `src/chronoagent/experiments/analysis.py` becomes `analysis/phase1.py` plus a new `__init__.py` that re-exports the public symbols. The CLI continues to import from `chronoagent.experiments.analysis` unchanged. (2) **Raw data is opt-in via `collect_raw=True`** so existing 10.6 runner tests + production runs that don't need plots pay zero cost. The default-off behaviour is locked in by `test_collect_raw_default_is_off`. (3) **Per-decision projection has a stable schema across detector flavours.** `_project_decision_for_raw` always emits `step_index`, `success`, `agent_id`, `score`, `flagged`, then only adds `bocpd_score` / `forecaster_score` / `integrity_score` when the source FullSystemDecision had them set. NoMonitoring decisions get `score=0.0` / `flagged=False` defaults so the plot layer's `getattr(d, "score", 0.0)` pattern works on every detector. (4) **Per-experiment vs root figures dir.** Per-run figures (signal drift) live under `<results>/<experiment>/figures/`; multi-experiment comparison figures live under `<results>/figures/`. Keeps the file tree readable when six experiments + a comparison root all coexist. (5) **Pinned `_EXPERIMENT_COLOURS` and `_EXPERIMENT_DISPLAY_NAMES` tables** so figures generated in different sessions are visually consistent across regenerations. (6) **Lazy matplotlib import with `matplotlib.use("Agg")`** centralised in `_setup_matplotlib()`. The plot test file calls `matplotlib.use("Agg")` at module import time before any pyplot import to lock the backend before pytest collects any matplotlib state. (7) **Autouse `_close_matplotlib` fixture** in the test file calls `plt.close("all")` after every test so figure-leaking failures don't cascade. (8) **`_empirical_roc` is a small helper** rather than a sklearn call because it needs to gracefully degenerate to the diagonal when one class is missing or scores are empty (sklearn would raise). (9) **AWT box plot draws hatched grey placeholder boxes for fully-empty AWT columns** (e.g. NoMonitoring) instead of crashing or skipping the experiment. The label below the box reports `n=0/num_runs` so the reader knows the column has no data. (10) **Ablation bar chart hides the CI error bar when `n < 2`** so a one-sample point does not misleadingly imply a confidence interval; matches the 10.6 `_aggregate_metric` single-sample policy. **Key gotchas locked in:** (a) **`matplotlib.use("Agg")` must run BEFORE the first pyplot import.** First test attempt put it inside the autouse fixture and got the warning that `use` was a no-op. Fix: import matplotlib at module top, call `use("Agg")`, then import the plot module under a `# noqa: E402` so the linter does not flag the import order. (b) **`ax.boxplot(..., labels=...)` is deprecated in matplotlib 3.10**, replaced with `tick_labels=...`. (c) **`ruff SIM105` enforces `contextlib.suppress` over a bare `try / except / pass`** for the AWT CSV cell parsing; rewrote the loop to use `contextlib.suppress(TypeError, ValueError)`. **Test file:** `tests/unit/test_experiment_plots.py` (~640 lines, 41 tests across 11 classes). `TestRunnerRawData` x10 (collect_raw default off, populated raw_runs shape, run_index/seed preservation, FullSystem subscores in raw decisions, NoMonitoring decisions have no subscores, raw_runs reset on second run, write skips raw when omitted, write persists raw when supplied, signals.npy round-trip, decisions.json round-trip). `TestLoadArtefacts` x4 (loader returns dataclass, raw data read when present, raw dir absent handled, missing aggregate.json raises). `TestEmpiricalRoc` x3 (empty returns diagonal, single class returns diagonal, perfect separation gives the corner-at-(0,1) curve). `TestPlotSignalDrift` x4 (PNG+SVG created, per-experiment figures dir, missing raw raises, run_index out of range raises). `TestPlotHealthScoreComparison` x4 (PNG+SVG, root figures dir, empty list rejected, missing raw raises). `TestPlotAwtBox` x4 (PNG+SVG, handles NoMonitoring with empty AWT column via hatched placeholder box, runs.csv only no raw required, empty list rejected). `TestPlotAllocationEfficiencyOverTime` x3 (PNG+SVG, missing raw raises, root figures dir). `TestPlotRocCurve` x2 (PNG+SVG, missing raw raises). `TestPlotAblationBarChart` x4 (PNG+SVG, no raw data required, NaN metrics handled (NoMonitoring AWT mean is null), empty list rejected). `TestGenerateAllPlots` x3 (12 paths returned for 6 figures x 2 formats, drift_experiment override, empty list rejected). **Files changed (1 git mv + 2 new src + 1 new test + 2 doc + edits):** rename `src/chronoagent/experiments/analysis.py` -> `src/chronoagent/experiments/analysis/phase1.py`; new `src/chronoagent/experiments/analysis/__init__.py` (24 lines, public re-export); new `src/chronoagent/experiments/analysis/plots.py` (~640 lines, 6 figure functions + loader + helpers); new `tests/unit/test_experiment_plots.py` (~640 lines, 41 tests across 11 classes); edit `src/chronoagent/experiments/experiment_runner.py` (added `RawRunRecord`, `collect_raw` flag, `raw_runs` property, `_project_decision_for_raw`, raw-data writing in `write_experiment_results`); edit `PLAN.md` (10.7 Findings/Challenges/Completed added, 10.7 ticked); edit `CLAUDE.md` (this table). **No new dependencies** -- matplotlib is already in `[project.dependencies]` from Phase 1. **Previous session carry-over:** 2026-04-11. **Phase 10 task 10.5 merged to `main` via PR #34 squash commit `5160b5b` (all 4 CI checks green on 3.11 + 3.12). Phase 10 task 10.6 shipped on branch `feat/phase10-task10.6-experiment-runner`, PR to open against `main`.** Local pre-push run for 10.6: ruff check + format --check clean on 125 files, mypy --strict --no-incremental clean on 77 source files, pytest 1388 passed (1338 prior + 50 new experiment-runner tests) 95.99% coverage, 3 intermittent ChromaDB cross-test-pollution flakes across two re-runs (different tests each run, all 125 pass in isolation, same flake pattern documented from Phases 5.7/8.3/9.1/9.2/9.3/9.4/9.6/10.1/10.4, not introduced by 10.6). **What 10.6 shipped:** Two new src modules under `src/chronoagent/experiments/`. `experiment_runner.py` (~570 lines) ships `ExperimentRunner` class plus `RunResult` / `MetricAggregate` / `AggregateResult` frozen dataclasses, a `SignalMatrixFactory` Protocol for pluggable signal sources, `default_signal_matrix_factory` wrapping `SignalValidationRunner` via a deferred import (so unit tests pay no ChromaDB cost at collection time), `_dispatch_detector`, `_compute_metrics`, `_aggregate_metric` (Student-t 95% CI via `scipy.stats.t.ppf`), and `write_experiment_results` which persists `runs.csv` + strict-JSON `aggregate.json`. `full_system_detector.py` (~460 lines) ships `FullSystemConfig` / `FullSystemDecision` / `FullSystemDetector` plus module constants `KL_COLUMN_INDEX = 3`, `ENTROPY_COLUMN_INDEX = 5`, `FULL_SYSTEM_AGENT_ID = "full_system_detector"`, and `FORECASTER_IS_PLACEHOLDER = True`. The detector is an offline replay of ChronoAgent's monitoring stack driven by three anomaly flags: `bocpd` runs the REAL project `BOCPD` implementation offline over the KL-divergence column (genuine measurement of the BOCPD contribution), `forecaster` runs a lightweight EMA-residual stand-in for Chronos (honest placeholder — the module flag + docstring make this explicit and a follow-up will replace it with real Chronos inference), `integrity` runs a MAD-outlier score on the entropy column as an offline stand-in for the memory integrity module. Sub-scores are averaged and flagged iff `combined > config.decision_threshold` (default 0.5, strict `>`). When every anomaly flag is off the detector falls back to Sentinel z-score, but in practice that only happens through the runner's dispatch table for `baseline_sentinel` which reaches the real `SentinelBaseline` class first. **Dispatch rule (locked in):** `cfg.name == "baseline_sentinel"` -> `SentinelBaseline(SentinelConfig(calibration_steps=cfg.attack.injection_step))`; `cfg.ablation.health is False` -> `NoMonitoringBaseline()`; otherwise -> `FullSystemDetector(cfg.ablation, FullSystemConfig(bocpd_hazard_lambda=cfg.system.bocpd_hazard_lambda, calibration_steps=cfg.attack.injection_step))`. Matches all six 10.5 YAMLs one-to-one. **Per-run flow:** `cfg.per_run_seed(run_index)` derives per-run seed -> factory produces `(num_prs, NUM_SIGNALS)` matrix -> detector emits decisions -> `_compute_metrics` projects decisions to dict form for `allocation_efficiency`, reads `flagged` + `score` via `getattr(..., default)` so NoMonitoring decisions (which have neither attribute) degrade gracefully to all-False predictions + all-zero scores, walks the decision list once to find the first-flagged step for `advance_warning_time`. Ground-truth labels are `[False]*injection_step + [True]*(num_prs - injection_step)`. **Aggregation:** per-metric mean / std / 95% CI using Student-t half-width; NaN dropped before aggregation; empty returns `(nan, nan, nan, nan, 0)`; single sample returns mean + NaN CI to avoid implying a bound from one observation. **Persistence:** `write_experiment_results(aggregate, output_dir)` writes `runs.csv` via DictWriter and `aggregate.json` with NaN scrubbed to `null` through a recursive `_nan_to_none` walker (json.dump(default=...) does NOT intercept Python floats, so a NaN would otherwise become the literal `NaN` token which is invalid JSON). **Signal matrix provenance:** The default factory wraps `SignalValidationRunner.create(n_steps=max(clean_len, poison_len), n_calibration=clean_len)` and truncates the symmetric clean + poisoned matrices into `(num_prs, NUM_SIGNALS)` with `clean[:injection_step]` vstacked with `poison[:num_prs - injection_step]`. Tests inject a fast `_shifted_factory()` that produces seeded gaussian noise + an additive shift on the KL + entropy columns after `injection_step`, so the 50 runner tests complete in ~7 s without touching ChromaDB. **Key design decisions locked in:** (1) **Two files not one.** Runner orchestration and the full-system detector are separable responsibilities; tests can import `FullSystemDetector` without pulling in `scipy.stats` and the metric machinery. (2) **`SignalMatrixFactory` Protocol + deferred `SignalValidationRunner` import.** The 50 runner tests never pay the ChromaDB startup cost; only the 3 `@pytest.mark.slow` integration tests use the real factory. (3) **`_DetectorLike.run` typed as `Sequence[Any]` instead of `Sequence[_DecisionLike]`** because mypy's invariant Sequence would reject concrete decision subclasses; the structural contract is enforced at projection time in `_compute_metrics`. (4) **`_compute_metrics` uses `getattr(d, "flagged", False)` / `getattr(d, "score", 0.0)`** so NoMonitoring decisions degrade gracefully; the 10.2 metric functions then translate all-False + all-zero into `detection_f1 = 0.0` and (via sklearn's tie-breaking convention) `detection_auroc = 0.5`, which is the honest signal for "no monitoring sees nothing". (5) **Student-t CI not normal approximation.** For `num_runs=5` the t critical value is ~2.78 vs the normal 1.96, a 42% wider interval; using the normal would systematically under-report uncertainty. (6) **NaN scrub via recursive walker, not `default=...`.** `json.dump(default=...)` does not intercept Python floats (they are "serialisable"), so the walker is the only way to produce strict JSON. (7) **`FORECASTER_IS_PLACEHOLDER = True` module flag + explicit docstring.** The forecaster channel is an EMA residual, NOT a real Chronos call. Makes the limitation impossible to miss and gives 10.7 / 10.8 follow-up work a clear replacement target. (8) **`pyproject.toml` grows a `markers = ["slow: ..."]` entry under `[tool.pytest.ini_options]`** so `@pytest.mark.slow` on the 3 integration tests does not emit `PytestUnknownMarkWarning`. **Key gotchas locked in:** (a) **`json.dump(default=...)` does not intercept native Python floats.** First NaN-serialisation attempt used `default=_json_default` but the test failed because NaN passed through the built-in float path. Fix: preprocess the payload with a recursive walker before `json.dump(..., allow_nan=False)`. (b) **ruff `SIM300` bans yoda conditions `CONST == pytest.approx(0.95)` but REQUIRES `pytest.approx(0.95) == CONST`** — the opposite of the usual yoda-condition lint. One test assertion needed rewriting. (c) **`mypy --strict` rejects `Sequence[Specific]` as a subtype of `Sequence[Protocol]` because Sequence is invariant.** Three dispatch returns flagged as incompatible until I widened `_DetectorLike.run` to `Sequence[Any]`. **Test file:** `tests/unit/test_experiment_runner.py` (~730 lines, 50 tests, 8 classes). `TestAggregateMetric` x8 (empty, single sample, uniform samples, symmetric CI, Student-t formula spot check, NaN dropping, all-nan input, CI_CONFIDENCE constant). `TestDispatchDetector` x8 (baseline_sentinel -> Sentinel, ablation_no_health_scores -> NoMonitoring, parametrized over the four full-system YAMLs -> FullSystemDetector, Sentinel calibration_steps matches injection_step, FullSystem hazard_lambda matches system config, FullSystem respects ablation flags). `TestComputeMetrics` x4 (perfect decisions give expected metrics, pre-injection firing gives positive AWT, never firing gives None AWT + 1.0 alloc eff + 0.0 F1, wrong decision count raises). `TestExperimentRunnerEndToEnd` x8 (full-system config yields expected metrics on shifted fake, baseline_sentinel real-YAML dispatch, ablation_no_health real-YAML dispatch, null factory never fires, per-run determinism, per-run seeds are `cfg.seed + run_index`, provenance includes every config block, injected factory is called once per run with correct kwargs). `TestResultDataclasses` x3 (RunResult frozen, MetricAggregate fields, AggregateResult run order). `TestFullSystemDetectorChannels` x12 (all channels on flags post-injection, BOCPD-only / forecaster-only / integrity-only / all-off fallback, agent_id rotates through `AGENT_IDS`, wrong shape rejected, too-few-rows rejected, zero-row matrix returns `[]`, FullSystemDecision frozen, FullSystemConfig validators reject every out-of-range field). `TestWriteExperimentResults` x4 (CSV + JSON round-trip, NaN metrics serialise as null, output directory created on demand, detector_name in CSV). `TestDefaultFactoryIntegration` x3 marked `@pytest.mark.slow` (end-to-end run on main_experiment at num_runs=1 / num_prs=16 / injection_step=8 via the real `SignalValidationRunner`, default factory rejects zero phase lengths, default factory rejects `attack_type='none'`). **Files changed (2 new src + 1 new test + 1 config + 2 doc):** `src/chronoagent/experiments/experiment_runner.py` (new, 570 lines), `src/chronoagent/experiments/full_system_detector.py` (new, 460 lines), `tests/unit/test_experiment_runner.py` (new, ~730 lines, 50 tests across 8 classes), `pyproject.toml` (added `slow` marker), `PLAN.md` (10.6 Findings/Challenges/Completed added, 10.6 ticked), `CLAUDE.md` (this table). **No new dependencies.** `scipy.stats.t` is already transitive; `numpy`, `pydantic`, `yaml`, `sklearn` already pinned. **Previous session carry-over:** 2026-04-11. **Phase 10 task 10.4 merged to `main` via PR #33 squash commit `e5d3220` (all 4 CI checks green on 3.11 + 3.12). Phase 10 task 10.5 shipped on branch `feat/phase10-task10.5-experiment-configs`, PR to open against `main`.** Local pre-push run for 10.5: ruff check + format --check clean on 122 files, mypy --strict --no-incremental clean on 75 source files (zero new src files), pytest 1338 passed (1232 prior + 106 new experiment-config tests) 95.77% coverage, zero flakes this run. **What 10.5 shipped:** Six experiment YAMLs under `configs/experiments/`, one per row of the PLAN.md 10.5 table: (1) `main_experiment.yaml` - MINJA + full system (every ablation flag `True`), the headline paper result; (2) `agentpoison_experiment.yaml` - AGENTPOISON + full system, the generalisation row; (3) `ablation_no_forecaster.yaml` - MINJA with `ablation.forecaster=false` and every other ablation flag `True`; (4) `ablation_no_bocpd.yaml` - MINJA with `ablation.bocpd=false` only; (5) `ablation_no_health_scores.yaml` - MINJA with `ablation.health=false` only; (6) `baseline_sentinel.yaml` - MINJA with every ablation flag `False`, marking the "full ChronoAgent stack off, runner dispatches the reactive comparator" hand-off. Every YAML carries full `attack` / `ablation` / `system` blocks even when the block matches defaults, so operators can read one file end-to-end without cross-referencing the schema, and the 10.6 runner can load every config through one code path. All six pin the shared reproducibility knobs identically: `seed=42`, `num_runs=5`, `num_prs=25`, `attack.target="both"`, `attack.injection_step=10`, `attack.n_poison_docs=10`, `attack.strategy="default"`, `system.llm_backend="mock"`, `system.bocpd_hazard_lambda=50.0`, `system.health_threshold=0.3`, `system.integrity_threshold=0.6`. **Key design decisions locked in:** (a) **Shared knob pinning is the honesty guarantee for the paper's ablation table.** Any delta between configs must be attributable ONLY to the ablation block (or the attack type for the generalisation row). `TestSharedReproducibilityKnobs` parametrizes a check per knob over all six configs so a drift in any shared knob trips a hard fail. (b) **One-knob delta per ablation row.** The three `ablation_no_*` YAMLs flip exactly one `AblationConfig` bool each. `test_one_knob_delta_vs_main` loops over the three files, computes the set difference against `main_experiment.ablation`, and asserts `len(differing) == 1`. Future additions (e.g. `ablation_no_integrity`) must satisfy the same invariant. (c) **`baseline_sentinel.yaml` resolves the "baseline-as-config vs baseline-as-runner-flag" question CLAUDE.md flagged at 10.4 end as BOTH.** The YAML is a valid `ExperimentConfig` (baseline-as-config) and the runner (10.6) takes the additional hint from `cfg.name == "baseline_sentinel"` + the four-false ablation pattern to dispatch `SentinelBaseline` rather than the normal pipeline (baseline-as-runner-flag). `test_baseline_sentinel_has_every_flag_off` locks in the four-false pattern, and `test_one_knob_delta_vs_main` explicitly excludes `baseline_sentinel` from its parametrize list so the one-knob invariant does not fire against the baseline row. (d) **No `ablation_no_integrity.yaml` and no `baseline_no_monitoring.yaml` shipped in this task.** PLAN.md's 10.5 table names exactly six configs; the `no_monitoring` baseline from task 10.4 is baseline-as-runner-flag only (10.6 will expose a CLI switch or a name convention), and the integrity ablation is left out because PLAN.md does not cite it in the paper's ablation table. `test_exactly_six_experiment_yamls_in_directory` gates against stray `*.yaml` additions so a future drive-by config cannot slip in without a deliberate review. (e) **`signal_validation.yaml` is the Phase 1 runner input and does NOT round-trip through `ExperimentConfig.from_yaml`.** The 10.5 test file explicitly excludes it by filename stem in every YAML enumeration so the strict `extra="forbid"` validator does not fire against a completely different schema. **Key gotchas locked in:** (1) **ruff format reshuffles set comprehensions onto one line if they fit.** The first draft of `test_exactly_six_experiment_yamls_in_directory` wrote the `yaml_files = {p.stem for p in CONFIG_DIR.glob(...) if ...}` across three lines; `ruff format` collapsed it onto one. Accept the rewrite. (2) **`ExperimentConfig.model_dump()` loses the Literal annotation of `attack.type`** (it is serialised as a plain str), so the round-trip test uses `ExperimentConfig.model_validate(cfg.model_dump()) == cfg` rather than asserting field-by-field; pydantic's `__eq__` handles the re-coercion. (3) **The `TestAttackTypePerPlanTable.test_attack_type_matches_plan` parametrize uses `sorted(EXPECTED_ATTACK_TYPES.items())`** for deterministic test ordering and `.ids` in pytest output; the same pattern is used in `TestAblationDeltasPerPlanTable.test_ablation_exact_match`. **Test file:** `tests/unit/test_experiment_configs.py` (350 lines, 106 tests, 7 classes). `TestConfigFilesExist` (7 tests: parametrized existence per config + one set-equality check on the directory). `TestRoundTripThroughSchema` (18 tests: `from_yaml` success, `cfg.name == filename`, `model_validate(model_dump()) == cfg`, each parametrized over six configs). `TestEveryNestedBlockExercised` (18 tests: attack/ablation/system block populated per config). `TestSharedReproducibilityKnobs` (36 tests: parametrized per-knob over six configs). `TestAttackTypePerPlanTable` (6 tests: attack type per config). `TestAblationDeltasPerPlanTable` (14 tests: exact ablation match per config + main + agentpoison full-system + one-knob-delta per ablation file + baseline_sentinel all-false + per-ablation detail checks). `TestPerRunSeedOverShippedConfigs` (6 tests: 5 distinct per-run seeds per config). **Files changed (6 new YAML + 1 new test + 2 doc):** `configs/experiments/main_experiment.yaml`, `configs/experiments/agentpoison_experiment.yaml`, `configs/experiments/ablation_no_forecaster.yaml`, `configs/experiments/ablation_no_bocpd.yaml`, `configs/experiments/ablation_no_health_scores.yaml`, `configs/experiments/baseline_sentinel.yaml` (all new), `tests/unit/test_experiment_configs.py` (new, 350 lines, 106 tests across 7 classes), `PLAN.md` (10.5 Findings/Challenges/Completed added, 10.5 ticked), `CLAUDE.md` (this table). No new dependencies, no new src/ files. **wincredman note:** the intermittent push wall from 10.4 did NOT reproduce; `git push -u origin feat/phase10-task10.4-no-monitoring-baseline` worked cleanly on first try and `gh auth status` reported the token healthy. The `feedback_wincredman_push_wall.md` memory has been updated to describe the failure as intermittent rather than sticky. **Previous session carry-over:** 2026-04-11. **Phase 10 task 10.4 shipped on branch `feat/phase10-task10.4-no-monitoring-baseline`, PR to open against `main`.** Local pre-push run for 10.4: ruff check + format --check clean on 121 files, mypy --strict --no-incremental clean on 75 source files (1 new: `baselines/no_monitoring.py`), pytest 1232 passed (1204 prior + 28 new no-monitoring tests) 95.77% coverage, zero flakes this run. **What 10.4 shipped:** New `src/chronoagent/experiments/baselines/no_monitoring.py` (~150 lines) holding the dumb round-robin comparator the Phase 10 paper's ablation table cites to isolate the contribution of *any* monitoring at all versus the Sentinel (10.3) and full-system conditions. Exports `NO_MONITORING_AGENT_ID = "no_monitoring_baseline"` (baseline-as-a-whole label, deliberately distinct from the canonical `AGENT_IDS`), frozen `NoMonitoringDecision` dataclass (`step_index: int`, `success: bool`, `agent_id: str`), and `NoMonitoringBaseline` class with `decide(step_index)` and `run(signal_matrix)` methods. `decide` cycles through `chronoagent.allocator.capability_weights.AGENT_IDS` (`planner` -> `security_reviewer` -> `style_reviewer` -> `summarizer`) via `AGENT_IDS[step_index % len(AGENT_IDS)]`, always reports `success=True`, and is deterministic with no randomness anywhere; two calls with the same inputs produce byte-identical decision lists regardless of seed. `run(signal_matrix)` validates the `(T, NUM_SIGNALS)` shape but deliberately *ignores* the matrix contents, so clean / shifted / NaN-filled inputs all produce identical decision lists (locked in by `test_signal_matrix_content_is_ignored`). `src/chronoagent/experiments/baselines/__init__.py` docstring updated to drop the "to be added" tag for the new module. **Key design decisions locked in:** (a) **Round-robin uses canonical `AGENT_IDS` order, not a shuffle or hash.** The tuple is already pinned by the Phase 5 capability matrix; using it verbatim gives reproducibility without threading a seed through the baseline. (b) **Every step succeeds by construction.** Without a detector, there is nothing to flag, so forcing `success=True` on every row surfaces the honest `allocation_efficiency = 1.0`, single-class `detection_auroc = nan`, and `detection_f1 = 0.0` signals the 10.2 metric functions already expect. Synthesising fake failures to make the baseline look worse would bake attack-model assumptions into the comparator. (c) **`NoMonitoringDecision.agent_id` carries the *real* dispatched agent id**, not the baseline-wide label; the module constant `NO_MONITORING_AGENT_ID` exists only for run-level tagging so aggregated allocator-audit CSVs can compute per-agent efficiency on the same column used by Sentinel and full-system runs. `test_distinct_from_canonical_agent_ids` locks in the non-collision. (d) **`run()` validates the matrix shape even though it does not read the contents**, matching the Sentinel 10.3 fail-fast surface so the runner (10.6) can drive both baselines through a single call site without branch-specific validation. (e) **Stateless, no calibration.** Unlike Sentinel there is no baseline window, no std floor, no z-threshold, no `RuntimeError` when called un-calibrated; deliberately the simplest possible comparator so the ablation table has a clean "monitoring contribution" delta. (f) **Zero-row matrix returns `[]`** (vacuously valid) so a runner that happens to pass an empty input does not error out. **Key gotchas locked in:** (1) **ruff's import sorter reshuffled the test file on the first save.** First draft had `import math` / `FrozenInstanceError` / third-party / first-party in my preferred order; `ruff check --fix` re-sorted them into a stable block order. Lesson: write tests assuming ruff will re-sort on format and do not hand-craft import block layouts. (2) **`NoMonitoringDecision` is intentionally permissive at the dataclass level about `success=False`.** The baseline as a whole always emits `success=True`, but the dataclass accepts either bool so tests and the runner can construct hand-crafted decisions for projection / aggregation checks without fighting the type. **Test file:** `tests/unit/test_no_monitoring_baseline.py` (280 lines, 28 tests, 5 classes). `TestNoMonitoringDecision` (3 tests: explicit field round-trip, frozen, `success=False` representable at the dataclass level). `TestNoMonitoringBaselineDecide` (8 tests: step 0 = first agent, step 1 = second agent, cycle wraps at `len(AGENT_IDS)`, full first cycle matches `AGENT_IDS` element-for-element over all four agents, every decision succeeds across 50 steps, determinism across two independent instances, negative step index rejected with `ValueError`, `step_index` recorded verbatim). `TestNoMonitoringBaselineRun` (9 tests: one decision per row on a 20-row matrix, monotonic step indices, 3-cycle agent sequence equals `list(AGENT_IDS) * 3`, all-success across 30 rows, zero-row matrix returns `[]`, content ignored across clean / shifted / NaN-filled inputs, wrong-rank rejected, wrong-column-count rejected, determinism across two calls). `TestNoMonitoringAgentIdConstant` (2 tests: label value is `"no_monitoring_baseline"`, distinct from `AGENT_IDS`). `TestNoMonitoringMetricsIntegration` (6 tests: `allocation_efficiency` = 1.0 on a 20-row run, `allocation_efficiency` = 1.0 on a length-1 run, `detection_auroc` = nan on all-zero labels + all-zero scores via the 10.2 single-class short-circuit, `detection_f1` = 0.0 on all-zero predictions against a 5-clean + 5-attack ground truth, `detection_f1` = 0.0 on fully-zero label + prediction streams (silent, no warnings), decision `agent_id` values span the full `AGENT_IDS` set and never contain `NO_MONITORING_AGENT_ID`). **Files changed (1 new src + 1 new test + 1 doc src + 2 doc):** `src/chronoagent/experiments/baselines/no_monitoring.py` (new, ~150 lines, 1 constant + 1 dataclass + 1 class + 2 public methods), `tests/unit/test_no_monitoring_baseline.py` (new, 280 lines, 28 tests across 5 classes), `src/chronoagent/experiments/baselines/__init__.py` (docstring edit: dropped the "to be added" note for the new module), `PLAN.md` (10.4 Findings/Challenges/Completed added, 10.4 ticked), `CLAUDE.md` (this table). No new dependencies. **Previous session carry-over:** **Phase 10 task 10.2 merged to `main` via PR #31 squash commit `7b314fd` (all 4 CI checks green after one mypy fix-up).** The fix-up was a one-line follow-up (`fix(mypy): drop now-redundant sklearn type-ignore`): my 10.2 change added `sklearn.*` to `[[tool.mypy.overrides]]` with `disable_error_code = ["import-untyped"]`, which made the existing inline `# type: ignore[import-untyped]` on `src/chronoagent/memory/integrity.py:54` redundant. CI's `mypy src/` run (strict, so `warn_unused_ignores = true`) flagged it with `[unused-ignore]`, even though my local `mypy --strict src/chronoagent/experiments/metrics.py` run passed cleanly (incremental cache masked it). Lesson: after touching `[[tool.mypy.overrides]]`, always run `mypy --no-incremental src/` before push, not just the changed file. **Phase 10 task 10.3 shipped on branch `feat/phase10-task10.3-sentinel-baseline`, PR to open against `main`.** Local pre-push run for 10.3: ruff check + format --check clean on 119 files, mypy --strict --no-incremental clean on 74 source files (2 new: `baselines/__init__.py` + `baselines/sentinel.py`), pytest 1204 passed (1167 prior + 37 new sentinel tests) 95.74% coverage, zero flakes this run. **What 10.3 shipped:** New subpackage `src/chronoagent/experiments/baselines/` with a one-line `__init__.py` module doc, and `sentinel.py` (~250 lines) holding the reactive Sentinel baseline: the comparator the Phase 10 paper tables in 10.8 cite against the full ChronoAgent stack. Exports `SENTINEL_AGENT_ID = "sentinel_baseline"` constant, frozen `SentinelConfig` dataclass (`calibration_steps >= 2` default 10, `z_threshold > 0` default 3.0, `min_std > 0` default 1e-6) with `__post_init__` validators, frozen `SentinelDecision` dataclass (`step_index`, `score`, `flagged`, `success`, `agent_id` default `SENTINEL_AGENT_ID`), and `SentinelBaseline` class with `calibrate`, `decide`, `run` methods. Calibration learns per-signal mean + `ddof=1` std from the first `calibration_steps` rows of a `(T, NUM_SIGNALS)` matrix, clamps std to `min_std` so constant MockBackend signals cannot divide by zero, then scoring computes `max(|vec - mean| / std)` across all 6 signals and flags when `score > z_threshold` (strict `>` so boundary ties pass). `success = not flagged`. The class is intentionally single-step and memoryless: no rolling window, no temporal state, no forecasting, no health score. `run(signal_matrix)` is the Phase 10 runner entry point: calibrate on the first `calibration_steps` rows, then score every row (including the calibration window, which never flags by construction with the default config) and return `list[SentinelDecision]` in step order. Decisions plug straight into the 10.2 metric functions: a one-line dict projection for `allocation_efficiency`, direct attribute access for `detection_auroc` (scores) and `detection_f1` (`int(d.flagged)`), and `next((d.step_index for d in decisions if d.flagged), -1)` for `advance_warning_time`. **Key design decisions locked in:** (a) **Reactive z-score thresholding stands in for "execution trace matching".** Without a concrete library of known-bad traces, the cleanest instantiation of a reactive single-step detector is: learn mean/std from a clean window, then at each new step compute max |z| against that fixed baseline and flag above a threshold. Matches classic signature-based IDS in spirit (compare observation to fixed fingerprint set, no temporal modelling). (b) **Calibration-window steps cannot flag with the default config.** The variance identity bounds `sum((x_i - mean)^2 / std^2) = N - 1` (where std uses `ddof=1`), so each individual z-score within the calibration window is bounded by `sqrt(N - 1)`. With `calibration_steps=10` this bound is exactly `sqrt(9) = 3.0`, and the strict `>` comparison against `z_threshold=3.0` lets the boundary pass. `test_clean_random_run_never_flags_calibration_window` locks this in empirically with random noise so a future switch to `>=` or a change to `ddof=0` or a lower default threshold cannot silently break the invariant. (c) **Strict `>` on the threshold comparison, not `>=`.** A step sitting exactly at the threshold should NOT flag: the threshold is the acceptance boundary. `test_boundary_exactly_threshold_does_not_flag` locks this in with a hand-crafted 10-row calibration matrix where col 0 has a known `ddof=1` std. (d) **`min_std` floor is non-negotiable for MockBackend compatibility.** Several signals produced by MockBackend (e.g. `tool_calls` = constant 2 in the Phase 1 runner) have zero variance in the calibration window. Without the floor, numpy divides by zero and every subsequent step gets `inf` z-scores, flagging everything. With `min_std = 1e-6`, constant columns still get a meaningful z-score but cannot blow up. `test_min_std_floor_handles_constant_column` locks in `std = 1e-3` when the entire calibration window is constant. (e) **`SentinelDecision` is a frozen dataclass, NOT a Mapping.** The runner projects to a dict (one-liner) when handing decisions to `allocation_efficiency` because `SentinelDecision(...)` should NOT accidentally satisfy the `Mapping` protocol and silently count fields other than `success`. `test_allocation_efficiency_accepts_decision_mapping` pins the projection shape explicitly. (f) **`agent_id` field on `SentinelDecision` defaults to `"sentinel_baseline"`** so aggregated allocator-audit CSVs from multiple experiment runs can disambiguate baseline rows from full-system rows on a single column, without the runner having to thread a label parameter through every function. **Key gotchas locked in:** (1) **ruff `B017` bans bare `pytest.raises(Exception)`.** First draft of the two `test_frozen` tests used `with pytest.raises(Exception):` with a trailing comment naming `FrozenInstanceError`; ruff demanded the specific exception class, so I imported `FrozenInstanceError` from `dataclasses` and used it directly. (2) **`mypy --no-incremental` is required after touching `[[tool.mypy.overrides]]`.** The 10.2 ship-then-CI-fail on `integrity.py:54` was the same class of cache bug; locked in as a pre-push step for 10.3 and every future task that touches pyproject.toml mypy overrides. **Test file:** `tests/unit/test_sentinel_baseline.py` (350 lines, 37 tests, 6 classes). `TestSentinelConfig` (7 tests: defaults, frozen, `calibration_steps >= 2`, zero rejected, `z_threshold > 0`, negative rejected, `min_std > 0`). `TestSentinelDecision` (4 tests: explicit fields, `agent_id` default, frozen, `success`/`flagged` independently settable at the dataclass level). `TestSentinelBaselineCalibrate` (8 tests: `is_calibrated` flip, wrong rank, wrong column count, too-few-rows, exactly-`calibration_steps` rows accepted, extra rows beyond window ignored, `min_std` floor handles constant column, calibrate is idempotent). `TestSentinelBaselineDecide` (8 tests: un-calibrated raises `RuntimeError`, accepts `StepSignals`, accepts numpy vector, wrong-shape rejected, zero-deviation gives score 0, boundary `score == z_threshold` does NOT flag (locks in strict `>` semantics), `success = not flagged`, score is max across signals -- col 0 at +5σ dominates col 1 at +1σ -> score = 5.0). `TestSentinelBaselineRun` (6 tests: one decision per row, step indices monotonic, clean random run never flags calibration window, shifted post-calibration block flags every shifted row, `success` mirrors `not flagged` across full run, constant-signal run is stable). `TestSentinelMetricsIntegration` (4 tests: `allocation_efficiency` accepts decision mapping and returns 0.5 on a 10 clean + 10 shifted matrix, `detection_auroc` = 1.0 on well-separated blocks (paper-quality assertion), `detection_f1` = 1.0 on same separation, first-flagged-step equals injection block start). **Files changed (2 new src + 1 new test + 2 doc):** `src/chronoagent/experiments/baselines/__init__.py` (new, subpackage module doc), `src/chronoagent/experiments/baselines/sentinel.py` (new, ~250 lines, 3 dataclasses + 1 class + 5 public methods), `tests/unit/test_sentinel_baseline.py` (new, 350 lines, 37 tests across 6 classes), `PLAN.md` (10.3 Findings/Challenges/Completed added, 10.3 ticked), `CLAUDE.md` (this table). No new dependencies. **Previous session carry-over:** 2026-04-11. **Phase 10 task 10.2 shipped on branch `feat/phase10-task10.2-metrics`, PR to open against `main`.** Local pre-push run for 10.2: ruff check + format --check clean on 117 files, mypy --strict clean on 72 source files, pytest 1167 passed (1116 prior + 51 new metric tests) 95.70% coverage, `src/chronoagent/experiments/metrics.py` 100% covered (50/50), zero flakes this run (the usual intermittent ChromaDB cross-test-pollution pattern did NOT trip). **What 10.2 shipped:** New `src/chronoagent/experiments/metrics.py` (200 lines) exporting a small `AllocationResult` type alias (`bool | Mapping[str, Any]`) plus four paper-facing metric functions the Phase 10 runner (10.6) and analysis layer (10.7 / 10.8) will consume directly. (1) `advance_warning_time(injection_step, detection_step) -> int` returns the signed step difference `injection_step - detection_step`. Per Pivot A, concurrent detection (`injection_step == detection_step`) returns `0` instead of NO-GO, and a negative return means the detector was late. Both arguments are validated `>= 0` with explicit `ValueError` messages; callers must filter runs where the detector never fired before computing AWT. Numpy int inputs (`np.int64`, etc.) are coerced to plain Python `int` via `int(...)` so the value survives JSON serialisation unchanged. (2) `allocation_efficiency(results) -> float` cumulative success rate of allocator decisions, accepting either a plain `Sequence[bool]` or a sequence of mapping-style audit rows (each with a `"success"` key). Empty sequence returns `0.0` (vacuously), mixed bool + mapping sequences work, missing-`"success"`-key raises `KeyError`, non-bool/non-Mapping elements raise `TypeError`. The mapping path uses `bool(item["success"])` so `0`/`1`-coded integer flags also count correctly. The `isinstance(item, bool)` branch comes before `isinstance(item, Mapping)` so a `True`/`False` literal cannot accidentally be interpreted as an empty mapping. (3) `detection_auroc(y_true, y_scores) -> float` is a thin wrapper around `sklearn.metrics.roc_auc_score` that short-circuits undefined inputs into `float('nan')` BEFORE calling sklearn: empty arrays, zero-size scores, or single-class `y_true` all return NaN without ever touching sklearn's validation. Perfect-ranking returns `1.0` exactly, perfectly-inverted ranking returns `0.0`, uninformative (all-same) scores return `0.5` via sklearn's tie-breaking convention. (4) `detection_f1(y_true, y_pred) -> float` wraps `sklearn.metrics.f1_score` with `zero_division=0.0` so all-zero predictions return `0.0` silently instead of emitting `UndefinedMetricWarning`, and short-circuits empty arrays into `float('nan')`. Both sklearn wrappers use a `warnings.catch_warnings()` + `simplefilter("ignore", UndefinedMetricWarning)` block around the sklearn call so a future sklearn version that starts emitting new undefined-metric warnings still produces a clean stdout, and both explicitly wrap the sklearn return in `float(...)` so the result is a plain Python `float` (not a numpy scalar) for JSON serialisation. **Key design decisions locked in:** (a) **Empty `allocation_efficiency` returns `0.0`, not `nan`.** Downstream cumulative tracking in the runner (10.6) benefits from a numeric value it can always append to a CSV; "success rate of zero decisions" is vacuously zero and the test asserts this. The other two nan-returning metrics (`detection_auroc`, `detection_f1`) use `nan` because they feed into statistical aggregation (`np.nanmean` across 5 seeds) where a zero would pollute the mean, whereas allocation efficiency aggregates WITHIN a single run. (b) **AUROC and F1 short-circuit before calling sklearn** for every case where sklearn would emit `UndefinedMetricWarning` or raise `ValueError`. Centralising the nan translation here means the runner (10.6) can build a DataFrame row with no defensive try/except. (c) **`AllocationResult` type alias accepts `bool | Mapping[str, Any]` rather than forcing a projection.** The task allocator's audit-trail rows are already dict-shaped; demanding callers project to booleans before calling `allocation_efficiency` would be extra ceremony for no gain, and the `isinstance` branch is a one-line check. The test file covers the mixed bool+mapping case explicitly. (d) **`pyproject.toml` mypy overrides extended with `sklearn.*`.** 10.2 is the first `src/` import of sklearn (the rest of the project uses numpy/scipy for stats), and sklearn ships without a `py.typed` marker, so `mypy --strict` flagged `[import-untyped]` until `sklearn.*` joined the existing ignore list in `[[tool.mypy.overrides]]` next to `ruptures.*`, `scipy.*`, `pandas.*`. No new dependencies; sklearn (>=1.5.0) is already pinned in `[project.dependencies]`. **Test file:** `tests/unit/test_experiment_metrics.py` (290 lines, 51 tests, 7 classes). `TestAdvanceWarningTime` (9 tests: early, concurrent, late, zero-zero, parametrized invariant over 5 pairs, numpy-int coercion to Python int, negative injection rejected, negative detection rejected). `TestAllocationEfficiencyBoolInput` (7 tests: all success, all failure, half-half, two-thirds, single success, single failure, empty returns 0.0). `TestAllocationEfficiencyMappingInput` (5 tests: mapping all success, mixed, extra fields ignored, missing key raises KeyError, truthy non-bool `"success": 1/0` counts). `TestAllocationEfficiencyMixedAndErrors` (3 tests: mixed bool+mapping, string element raises TypeError, None element raises TypeError). `TestDetectionAuroc` (12 tests: perfect ranking = 1.0, inverted = 0.0, uninformative = 0.5, partial overlap bounded, accepts Python lists, empty y_true = nan, empty y_scores = nan, single-class all-zero = nan, single-class all-one = nan, all-zero scores with two classes = 0.5, return type is Python float, nan return type is Python float). `TestDetectionF1` (12 tests: perfect predictions = 1.0, all wrong = 0.0, half correct (TP=1 FP=1 FN=1) = 0.5, Python lists accepted, empty y_true = nan, empty y_pred = nan, all-zero true+pred = 0.0 silently, all-zero true + all-one pred = 0.0, single-class all-one exact match = 1.0, no warnings on zero division asserted via `warnings.simplefilter("error", UndefinedMetricWarning)`, return type is Python float, nan return type is Python float). **Files changed (1 new src + 1 new test + 1 config + 2 doc):** `src/chronoagent/experiments/metrics.py` (new, 200 lines, 4 functions + AllocationResult type alias), `tests/unit/test_experiment_metrics.py` (new, 290 lines, 51 tests across 7 classes), `pyproject.toml` (added `sklearn.*` to the `[[tool.mypy.overrides]]` ignore list), `PLAN.md` (10.2 Findings/Challenges/Completed added, 10.2 ticked), `CLAUDE.md` (this table). No new dependencies. **Previous session carry-over:** 2026-04-11. **Phase 9 task 9.6 merged to `main` via PR #29 squash commit `d37fac2` (all 4 CI checks green) -- Phase 9 (production hardening) is now CLOSED end-to-end with all six tasks merged. Phase 10 task 10.1 shipped on branch `feat/phase10-task10.1-experiment-config-schema`, PR opened against `main`.** Local pre-push run for 10.1: ruff check + format --check clean on 114 files, mypy --strict clean on 71 source files, pytest 1114 passed (1065 prior + 51 schema tests, with 2 pre-existing ChromaDB cross-test-pollution flakes that pass in isolation -- documented from Phases 5.7 / 8.3 / 9.1 / 9.2 / 9.3 / 9.4 / 9.6) 95.62% coverage. **What 10.1 shipped:** New `src/chronoagent/experiments/config_schema.py` (255 lines) with four pydantic models. (1) `ExperimentConfig` -- top-level: `name: str` (ASCII-safe identifier validator, allows `[A-Za-z0-9_-]` only), `seed: int >= 0`, `num_runs: int >= 1` default 5, `num_prs: int >= 1` default 25, plus nested `attack: AttackConfig`, `ablation: AblationConfig` (factory default), `system: SystemConfig` (factory default). (2) `AttackConfig` -- `type: Literal["minja", "agentpoison", "none"]`, `target: Literal["security_reviewer", "summarizer", "both"]` default `"both"`, `injection_step: int >= 0`, `n_poison_docs: int >= 0` default 10, `strategy: str` free-form descriptor default `"default"` with non-empty validator. (3) `AblationConfig` -- four bool toggles `forecaster` / `bocpd` / `health` / `integrity` all default `True`. (4) `SystemConfig` -- `llm_backend: Literal["mock", "together", "ollama"]` default `"mock"`, `bocpd_hazard_lambda: float > 0.0` default 50.0, `health_threshold: float in [0,1]` default 0.3, `integrity_threshold: float in [0,1]` default 0.6. Every model uses `model_config = ConfigDict(extra="forbid")` so a YAML key typo hard-fails at load time instead of silently shadowing. **Loaders:** `ExperimentConfig.from_yaml(path: str | Path) -> ExperimentConfig` is the single entry point. Reads via `yaml.safe_load`, checks the root is a mapping (raises `ValueError` if not), and calls `cls.model_validate(raw)` to surface pydantic `ValidationError` for any field failure. `FileNotFoundError` propagates from the missing-path check. **Helper:** `per_run_seed(run_index: int) -> int` returns `seed + run_index` so a single `ExperimentConfig` can drive `num_runs` reproducible runs without re-loading the YAML; raises `ValueError` for `run_index < 0` or `>= num_runs`. **Key design decisions locked in:** (a) **Schema is intentionally narrow.** Captures only the parameters PLAN.md task 10.1 enumerates plus the minimum needed to make YAML loading work. Anything that lives in `chronoagent.config.Settings` and is unaffected by an experiment (database URL, log format, etc.) is left to the application defaults. (b) **`name` field uses an ASCII-safe identifier validator.** Rejects whitespace, dots, slashes, shell metacharacters because the runner (10.6) splices `name` into output filenames; allowing arbitrary strings would create a path-traversal risk and break filename hashing. (c) **`strategy` validator only enforces non-emptiness, not whitespace stripping.** Silent input mutation is a footgun (operators looking at result rows would be confused why their YAML value differs from the recorded one), so the validator only rejects whitespace-only strings and passes everything else through verbatim. (d) **`extra="forbid"` everywhere.** A YAML key typo hard-fails at load time, not at measurement time three minutes into a run. (e) **`per_run_seed` is a method on the model, not a free function.** Keeps the seed-derivation logic colocated with the seed field so future changes (e.g. switching from `seed + run_index` to a hash-based scheme) only need to touch one place. **Test file:** `tests/unit/test_experiment_config_schema.py` with 51 tests across 6 classes: `TestAttackConfig` (10 tests: minimal construction, all 3 attack types, all 3 targets, unknown type rejection, unknown target rejection, negative injection step rejection, negative n_poison_docs rejection, empty strategy rejection, whitespace-strategy passes through verbatim, extra-field rejection). `TestAblationConfig` (3 tests: defaults full system, individual flags can be disabled, extra-field rejection). `TestSystemConfig` (7 tests: defaults are mock backend, all 3 backends accepted, unknown backend rejection, zero hazard lambda rejection, health threshold > 1 rejection, integrity threshold < 0 rejection, extra-field rejection). `TestExperimentConfig` (10 tests: minimal construction, full construction with every default overridden, parametrized unsafe-name rejection over 5 cases (`""`, `"   "`, `"with space"`, `"../escape"`, `"weird!"`), parametrized safe-name acceptance over 4 cases (`main`, `main_experiment`, `ablation-no-bocpd`, `v2_run`), negative seed rejection, zero num_runs rejection, zero num_prs rejection, extra-top-level-field rejection). `TestFromYaml` (6 tests: round-trip minimal, round-trip full with every nested field, missing file `FileNotFoundError`, non-mapping root `ValueError`, typo `ValidationError`, accepts `str` path argument). `TestPerRunSeed` (4 tests: run zero returns master seed, increments by run index, negative run index rejection, run index >= num_runs rejection). **Files changed (1 new src + 1 new test + 2 doc):** `src/chronoagent/experiments/config_schema.py` (new, 255 lines, 4 pydantic models + 2 helper methods), `tests/unit/test_experiment_config_schema.py` (new, 285 lines, 51 tests across 6 classes), `PLAN.md` (10.1 Findings/Challenges/Completed added, 10.1 ticked), `CLAUDE.md` (this table). No new dependencies; pyyaml, pydantic, and pydantic-settings are already in the runtime extras. **Previous session carry-over:** Phase 9 task 9.5 merged to `main` via PR #28 squash commit `d8d2ba6` (all 4 CI checks green). Phase 9 task 9.6 shipped on branch `feat/phase9-task9.6-package-build`, PR opened against `main`. Phase 9 is now CLOSED end-to-end -- all six tasks (9.1 rate limiter, 9.2 retry policies, 9.3 graceful degradation, 9.4 health endpoint, 9.5 logging audit, 9.6 package build) merged or in review.** Local pre-push run for 9.6: ruff check + format --check clean on 112 files, mypy --strict clean on 70 source files, pytest 1065 passed (1055 prior + 10 packaging tests) 95.57% coverage, zero flakes this run. **What 9.6 shipped:** Two changes: (1) `pyproject.toml` pinned `hatchling>=1.26.0` in `[build-system].requires` and added a new `[tool.hatch.build.targets.sdist]` block with an explicit whitelist (`/src`, `/tests`, `/docs`, `/configs`, `/alembic`, plus top-level config files like `/Makefile`, `/Dockerfile`, `/docker-compose.yml`, `/requirements.lock`, `/.env.example`, `/.pre-commit-config.yaml`, `/alembic.ini`, `/pyproject.toml`, `/README.md`) and a defensive exclude list (`**/__pycache__`, `**/*.py[cod]`, `**/.pytest_cache`, `**/.mypy_cache`, `**/.ruff_cache`, `**/.hypothesis`, `**/htmlcov`, `**/.coverage`, `**/dist`, `**/build`, `**/*.egg-info`). (2) `tests/unit/test_package_build.py` (new, 10 tests, 3 classes) locks in the packaging invariants without shelling out to `py -m build` per test. **Build verification (one-shot, not in CI):** `py -m build` produced `dist/chronoagent-0.1.0.tar.gz` (135 entries, down from thousands in the baseline) and `dist/chronoagent-0.1.0-py3-none-any.whl` (76 entries). Wheel inspection confirmed `chronoagent/dashboard/static/index.html` is bundled at 28556 bytes, `entry_points.txt` registers `chronoagent = chronoagent.cli:app`, and `WHEEL` metadata says `hatchling 1.29.0` + `Tag: py3-none-any`. **Throwaway-venv smoke test:** `py -m venv /tmp/chronoagent_smoke && /tmp/chronoagent_smoke/Scripts/python.exe -m pip install dist/chronoagent-0.1.0-py3-none-any.whl` installed cleanly; `chronoagent.exe --help` listed all three CLI commands (`serve`, `run-experiment`, `check-health`); `from chronoagent.dashboard import INDEX_HTML; INDEX_HTML.exists()` returned `True` with the expected 28556-byte size; `from chronoagent.main import create_app` imported cleanly. Cleaned up the venv + dist directory after the smoke test so the working tree stays clean. **Key design decisions locked in:** (a) **Tighten the sdist, leave the wheel alone.** The wheel was already correct because hatchling's `packages = ["src/chronoagent"]` walks every file (not just `*.py`) under the listed package; only the sdist needed a fence. (b) **Hatchling pinned at `>=1.26.0`** so future hatchling default-behaviour changes cannot silently break the build. (c) **Grafana JSON stays in `docs/`, NOT bundled into the wheel.** PLAN.md 9.6 wording only requires wheel + entry point; bundling docs/grafana would need either a refactor (move file under `src/chronoagent/`) or hatchling `force-include` (split file location vs wheel location). Both add complexity for an asset most ops teams deploy from the repo checkout. Punted from 9.6 scope; my session-note expansion that mentioned grafana was over-reach. (d) **No `LICENSE` file created.** `pyproject.toml` declares `license = { text = "MIT" }` for metadata; creating an actual LICENSE file is a project / legal decision and was punted. The sdist whitelist deliberately does NOT reference `/LICENSE` so the build does not error on the missing path. (e) **Test does not shell out to `py -m build`.** A real isolated build takes 30+ seconds and would slow CI significantly; the regression test asserts pyproject invariants (hatchling pin, entry point, wheel packages list, sdist include + exclude lists) and the importable bundled-asset path constants instead. Enough to catch the failure modes without paying the build cost. **Key gotchas locked in:** (1) Hatchling's sdist default is "ship everything not in `.gitignore`", which on this repo picks up `.claude/`, `.hypothesis/` (~1500 cached files), `chronoagent.db`, and one personal-notes file. Solved by the explicit whitelist. (2) `tarfile` is the right tool for sdist inspection, not `python -m zipfile`: sdist is `.tar.gz`, not zip, so `py -m zipfile -l dist/chronoagent-0.1.0.tar.gz` errors with "File is not a zip file". Used `py -c "import tarfile; ..."` to enumerate entries. (3) The first sdist whitelist attempt included `/LICENSE` and would have errored on missing path; removed before commit. **Test file:** `tests/unit/test_package_build.py` with 10 tests across 3 classes. `TestBundledDashboardAsset` (4 tests): `test_static_dir_resolves`, `test_index_html_resolves`, `test_index_html_has_dashboard_landmark`, `test_index_html_lives_inside_chronoagent_package` (asserts the file's resolved path has `chronoagent` package root as a parent so the wheel's `packages = ["src/chronoagent"]` rule will pick it up). `TestPyprojectPackagingInvariants` (5 tests): `test_hatchling_is_pinned_with_minimum`, `test_console_script_entry_point` (`chronoagent.cli:app`), `test_wheel_packages_listed`, `test_sdist_include_list_present` (every entry must start with `/` so hatchling does not walk subdirectories), `test_sdist_exclude_list_covers_local_artefacts`. `TestVersionStringMatches` (1 test) asserts `chronoagent.__version__` agrees with `pyproject.toml::project.version`. **Files changed (1 edited + 1 new test + 2 doc):** `pyproject.toml`, `tests/unit/test_package_build.py` (new, 175 lines, 10 tests), `PLAN.md` (9.6 Findings/Challenges/Decisions added, 9.6 ticked, Completed row updated), `CLAUDE.md` (this table; phase tracker flipped Phase 9 to `[x]`). No new dependencies; `tomllib` is stdlib in Python 3.11+. **Previous session carry-over:** Phase 9 task 9.4 merged via PR #27 squash commit `17bff0b`. Phase 9 task 9.5 merged via PR #28 squash commit `d8d2ba6`. Local pre-push run: ruff check + format --check clean on 111 files, mypy --strict clean on 70 source files, pytest 1055 passed (1037 prior + 18 audit tests) 95.57% coverage, zero flakes reproduced this run (the usual intermittent ChromaDB cross-test-pollution pattern did NOT trip). **What 9.5 shipped:** Pure audit task, no new runtime behaviour. Walked every `src/` module and routed logger acquisition through the single `chronoagent.observability.logging.get_logger` entry point. **Stdlib-backed migrations (4 files):** `src/chronoagent/allocator/task_allocator.py`, `src/chronoagent/scorer/health_scorer.py`, `src/chronoagent/scorer/chronos_forecaster.py`, `src/chronoagent/messaging/redis_bus.py` had their `import logging` + `logging.getLogger(__name__)` replaced with `from chronoagent.observability.logging import get_logger` + `logger = get_logger(__name__)`. Every `%s`-style positional call was rewritten into structured `logger.warning("event_name_snake_case", key=value, ...)` form. Event names (`task_allocator_round_robin_fallback`, `task_allocator_cached_health`, `task_allocator_malformed_health_dict`, `task_allocator_unexpected_health_type`, `health_scorer_unexpected_message_type`, `health_scorer_malformed_signal_payload`, `health_scorer_update`, `redis_bus_decode_failed`, `redis_bus_handler_error`, `chronos_forecaster_history_too_short`, `chronos_forecaster_forecast_failed`, `chronos_forecaster_loaded`, `chronos_forecaster_load_failed`) use snake_case with no trailing punctuation. **Direct-structlog migrations (11 files):** `src/chronoagent/escalation/audit.py`, `src/chronoagent/escalation/escalation_manager.py`, `src/chronoagent/pipeline/graph.py`, `src/chronoagent/observability/metrics_wiring.py`, plus the seven routers (`api/routers/dashboard.py`, `health_scores.py`, `escalation.py`, `metrics.py`, `memory.py`, `review.py`, `signals.py`). Each had `import structlog` + `logger: structlog.BoundLogger = structlog.get_logger(__name__)` swapped for `from chronoagent.observability.logging import get_logger` + `logger = get_logger(__name__)`, with the now-unused `structlog` import dropped. Behaviour is identical (the wrapper just forwards), but centralising the entry point means future logging-config changes only touch `observability/logging.py`. **Exemptions locked in:** (1) `src/chronoagent/retry.py` keeps `_logger = logging.getLogger("chronoagent.retry")` because tenacity's `before_sleep_log` hook requires a real `logging.Logger` instance. A block comment names the 9.2/9.5 decision. (2) `src/chronoagent/observability/logging.py` defines the wrapper and configures the stdlib root handler, so its `logging.getLogger()` call is the bootstrap. These are the ONLY two `logging.getLogger` calls left in `src/`, enforced by `TestNoStrayStdlibLoggers`. **New `tests/conftest.py`:** Session-scoped autouse fixture `_configure_structlog_for_tests` calls `configure_logging("test")` once per session. This is non-optional: structlog's default factory is a `PrintLogger` that writes to stdout and bypasses stdlib logging entirely, so `caplog` cannot capture structured events until `configure_logging` has been called. Four existing `test_task_allocator.py` assertions (`test_malformed_dict_is_logged_and_dropped`, `test_unknown_payload_type_is_logged_and_dropped`, `test_generic_exception_triggers_fallback`, `test_timeout_error_triggers_fallback`) started failing with empty `caplog.records` after the migration and were only fixable by forcing the stdlib bridge via the conftest fixture. Their assertion strings were also updated from the old human-readable prefixes (`"do not match HealthUpdate"`, `"falling back to round-robin"`) to the new event names (`"task_allocator_malformed_health_dict"`, `"task_allocator_round_robin_fallback"`). **Test file:** `tests/unit/test_logging_audit.py` with 18 tests across 5 classes. `TestNoStrayStdlibLoggers` (3 tests: regex scan forbidding `logging.getLogger` outside `retry.py` + `observability/logging.py`, regex scan forbidding `structlog.get_logger` outside `observability/logging.py`, positive assertion that `retry.py` still uses stdlib). `TestGetLoggerImport` (1 test: walks every importable submodule via `pkgutil.walk_packages`, checks the module-level `logger` attribute is structlog-backed, accepting `BoundLoggerLazyProxy` from the lazy factory and rejecting only `logging.Logger`). `TestConfigureLoggingAllEnvs` (3 tests parametrized over `dev`/`test`/`prod`: handler count is exactly 1 per env, handler uses `structlog.stdlib.ProcessorFormatter`, switching envs is idempotent). `TestGetLoggerRoundTrip` (3 tests: returns non-None, accepts kwargs at every log level, `.exception` inside an `except` block works). `TestStructuredFieldConvention` (4 tests parametrized over the 4 migrated stdlib-backed modules: regex check that no `logger.foo("... %s", x)` calls survive). **Key gotchas locked in:** (1) `structlog.get_logger` returns a `BoundLoggerLazyProxy`, not a `BoundLogger` or a subclass of `BoundLoggerBase`; the audit test checks `type(logger).__module__.startswith("structlog")` instead of `isinstance(logger, BoundLogger)`. (2) `logger.warning("msg with %s", x)` under a structlog bound logger silently drops the positional `x` and prints the `%s` literal; the regex spot check (`TestStructuredFieldConvention._PERCENT_S_PATTERN`) forbids this pattern in the migrated modules. (3) `caplog` is useless for structlog output unless `configure_logging` has been called at least once; the `tests/conftest.py` autouse fixture makes this universal. **Files changed (16 edited + 3 new + 2 doc):** edits: 4 stdlib migrations (`task_allocator.py`, `health_scorer.py`, `chronos_forecaster.py`, `redis_bus.py`), 11 direct-structlog migrations (`audit.py`, `escalation_manager.py`, `pipeline/graph.py`, `metrics_wiring.py`, 7 routers), `retry.py` (comment only), `tests/unit/test_task_allocator.py` (4 assertions updated to new event names); new: `tests/conftest.py` (28 lines, session-scoped configure_logging fixture), `tests/unit/test_logging_audit.py` (220 lines, 18 tests), `PLAN.md` (9.5 Findings/Challenges/Decisions added, 9.5 ticked, Completed row updated), `CLAUDE.md` (this table). No new dependencies, no runtime behaviour change. **Previous session carry-over:** Phase 9 task 9.3 merged to `main` via PR #26 squash commit `ef407f2` (all CI green on 3.11 + 3.12). Phase 9 task 9.4 shipped on branch `feat/phase9-task9.4-health-endpoint`, PR opened against `main`.** Local pre-push run for 9.4: ruff check + format --check clean on 109 files, mypy --strict clean on 70 source files, pytest 1037 passed 95.57% coverage, `src/chronoagent/api/health.py` 100% covered (67/67), `src/chronoagent/api/middleware.py` 100% covered (111/111). One earlier run surfaced 4 intermittent ChromaDB cross-test-pollution failures (`test_memory_quarantine::test_embedding_preserved_round_trip`, `test_memory_router::TestQuarantineDocs::test_with_reason`, plus several `test_agents::*` retrieve/synthesize tests); all passed in isolation, same pattern documented from Phases 5.7 / 8.3 / 9.1 / 9.2 / 9.3. A rerun in the same session with no code changes produced 1037 passed and 0 failed. **What 9.4 shipped:** Two files changed plus one new test. `src/chronoagent/api/health.py` grew from a 27-line liveness probe into a ~225-line comprehensive router that keeps the old `GET /health` route intact (legacy liveness probe) and adds `GET /api/v1/health` on the same `APIRouter`. New pydantic response models `ComponentReport` (`name: str`, `mode: Literal["primary","fallback","unavailable"]`, `detail: str`) and `HealthReport` (`status: Literal["healthy","degraded","unhealthy"]`, `version: str`, `components: dict[str, ComponentReport]`) give FastAPI a stable `response_model` + OpenAPI shape. `_aggregate_status(statuses)` applies strict precedence (`unavailable` dominates `fallback` dominates `primary`) with a short-circuit on the first `unavailable`; an empty iterable is treated as `healthy`. The handler reads `getattr(request.app.state, "component_status", {})` (populated by the 9.3 `_build_*` helpers) for the infrastructure components, always seeds an `api` self-entry (`primary: responding`), then layers on `_probe_together_ai(settings)` (always included) and `_probe_ollama(settings)` (only when `settings.llm_backend == "ollama"`). **LLM probes are config-only by design** so polling a health endpoint never burns paid API quota or adds 100 ms+ of network latency: Together inactive -> `primary: inactive (active backend: mock)`, Together active + empty `CHRONO_TOGETHER_API_KEY` -> `unavailable`, Together active + key set -> `primary: configured (model: ...)`. Same three-branch logic for Ollama keyed on `settings.ollama_base_url`. HTTP codes: `healthy` / `degraded` -> 200, `unhealthy` -> 503 (set via `response.status_code = 503` before return so FastAPI still applies `response_model`). `src/chronoagent/api/middleware.py`: default `RateLimitConfig.exempt_paths` grew from `("/health", "/metrics")` to `("/health", "/api/v1/health", "/metrics")` so Prometheus-style polling of the comprehensive endpoint cannot starve the 60-GETs-per-minute per-client budget. **Key design decisions locked in:** (a) One router file, two routes, same `APIRouter`. (b) The overall degraded/unhealthy label lives in `body.status`; HTTP code is just a coarse "ok vs broken" signal for dumb probes. (c) No network I/O from health probes; a future `?probe=true` query param can opt in. (d) `dev`/`test` envs are expected to run without `chronos-forecasting` installed, so the default test env rolls up as `degraded` via `forecaster: fallback` — this is the honest signal operators should see. The "healthy" test path uses a `force_chronos_available` fixture that monkeypatches `chronoagent.main.ChronosForecaster` with a stub whose `.available` returns `True`; a dedicated `TestEndpointDegradedOnForecaster` locks in the honest default. (e) `together_ai` is ALWAYS in the component list; `ollama` is conditional on `settings.llm_backend == "ollama"`. **Test file:** `tests/unit/test_health_endpoint.py` (330 lines, 26 tests, 10 classes): `TestAggregateStatus` (empty, all-primary, fallback-downgrades, unavailable-dominates, unavailable-short-circuits), `TestProbeTogetherAi` (inactive, active-no-key, active-key-set), `TestProbeOllama` (configured, empty), `TestBuildReportHelpers` (api-always-present, ollama-excluded, ollama-included), `TestEndpointHealthy` (5 under `force_chronos_available`: 200, healthy label, six-component set, version, schema round-trip), `TestEndpointDegradedOnForecaster` (default env rolls up degraded), `TestEndpointDegraded` (monkeypatched `make_engine` -> database fallback -> degraded -> 200; detail mentions `RuntimeError`), `TestEndpointUnhealthy` (together active + no key -> 503, unavailable dominates primary bus/database), `TestLegacyHealthEndpoint` (regression on `/health`), `TestRateLimitExemption` (exempt_paths contains `/api/v1/health`, 120 requests never 429). **Files changed (2 edited + 1 new test + 2 doc):** `src/chronoagent/api/health.py`, `src/chronoagent/api/middleware.py`, `tests/unit/test_health_endpoint.py` (new), `PLAN.md` (9.4 Findings/Challenges/Decisions added, 9.4 ticked, Completed row updated), `CLAUDE.md` (this table). No new dependencies. **Previous session carry-over:** Phase 9 task 9.3 shipped on branch `feat/phase9-task9.3-graceful-degradation`, PR opened against `main`.** Local pre-push run: ruff check + format --check clean on 108 files, mypy --strict clean on 70 source files, pytest 1010 passed 95.50% coverage, `src/chronoagent/main.py` 100% covered (123/123) and `src/chronoagent/observability/components.py` 100% covered (9/9). Pre-existing ChromaDB cross-test-pollution flakes tripped the full run: `test_memory_router.py::TestApproveDocs::test_idempotent_second_approve` once under coverage and `test_memory_quarantine.py::TestApprove::test_quarantine_metadata_stripped_on_approve` once without coverage; both passed in isolation and when running their own file alone; same cross-test ordering flake documented from Phases 5.7, 8.3, 9.1, and 9.2; not introduced by 9.3. **New module:** `src/chronoagent/observability/components.py` exports a frozen `ComponentStatus` dataclass (`name: str`, `mode: Literal["primary","fallback","unavailable"]`, `detail: str`) plus a `ComponentMode` literal alias. Intentionally tiny and hashable so the 9.4 health endpoint can embed it as-is in its response. **main.py refactor:** Four module-level helpers return `(instance, ComponentStatus)` tuples; lifespan calls each one and stuffs the status into `app.state.component_status: dict[str, ComponentStatus]` keyed by logical component name. (1) `_build_message_bus(settings)`: dev/test returns `LocalBus(), primary`; prod imports `RedisBus` inside the try, constructs it, then calls `bus._client.ping()` (single-shot probe with `# noqa: SLF001`) because `redis.Redis.from_url` is lazy and the constructor never fails on an unreachable host. Any `Exception` (including `ImportError`) yields `LocalBus, fallback` with `detail=f"local bus (redis unavailable: {type(exc).__name__})"`. (2) `_build_database(settings)`: runs `make_engine(settings)` -> `conn.execute(text("SELECT 1"))` -> `Base.metadata.create_all(engine)` inside the try; any failure logs `db_fallback` and builds an in-memory SQLite engine via `create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False}, poolclass=StaticPool)` with `create_all`. StaticPool is critical: without it every new connection gets a fresh private in-memory DB and tables vanish between the probe and the first query. Detail string embeds primary URL + exception class name. (3) `_build_chromadb()`: wraps `chromadb.EphemeralClient()`; there is no real fallback so failure re-raises after a log, but the helper still labels success as `primary: ephemeral (in-process)` so 9.4 can read it uniformly. (4) `_probe_forecaster()`: constructs a throwaway `ChronosForecaster()` and reads `.available` (uses `importlib.util.find_spec`, no model load); primary if chronos-forecasting is installed, fallback `BOCPD-only (chronos-forecasting not installed)` otherwise. Deliberately does NOT reach into `TemporalHealthScorer._chronos` to avoid crossing a private boundary for a sub-millisecond lookup. **Lifespan ordering:** `component_status` dict created first on `app.state`; `_build_database` called before the pipeline; `_build_chromadb` called before memory stores (chroma client feeds both `MemoryStore` and `QuarantineStore`); `_build_message_bus` called before the health scorer, escalation handler, and metrics wiring; `_probe_forecaster` called last since it's a pure probe. All four status entries land on `app.state.component_status` by the time `yield` runs. **Test file:** `tests/unit/test_graceful_degradation.py` with 22 tests across 8 classes: `TestComponentStatus` (frozen, hashable, field preservation), `TestBuildMessageBusDevTest` (dev + test envs use LocalBus as primary), `TestBuildMessageBusProd` (redis unreachable via ping -> fallback, constructor raises -> fallback, ping succeeds -> primary), `TestBuildDatabase` (valid sqlite primary, schema created, broken URL -> in-memory fallback via monkeypatch on `chronoagent.main.make_engine`, StaticPool enables cross-thread reads in the fallback engine), `TestBuildChromadb` (primary ephemeral, construction failure re-raises), `TestProbeForecaster` (monkeypatches `chronoagent.main.ChronosForecaster` with a stub that returns True/False on `.available`), `TestComponentStatusOnAppState` (real `create_app` in a `TestClient` context proves the four keys land on app.state, test env bus/database are primary, forged prod env with a fake RedisBus surfaces bus fallback, forged broken `make_engine` surfaces database fallback AND `/health` still returns 200 end-to-end). **Key test patterns locked in:** (a) Tests monkeypatch at the call site in `chronoagent.main` (e.g. `main_module.make_engine`, `main_module.ChronosForecaster`) rather than at the definition site in `chronoagent.db.session` / `chronoagent.scorer.chronos_forecaster`, because those symbols are imported into `main.py` at module import time and the helper functions reference the local name. (b) For RedisBus, tests monkeypatch `chronoagent.messaging.redis_bus.RedisBus` directly because `_build_message_bus` does a lazy `from chronoagent.messaging.redis_bus import RedisBus` inside the try/except. (c) The broken-database test asserts BOTH the fallback status label AND that `/health` returns 200 inside the same `with TestClient(app) as client:` context to prove the app actually boots on the fallback engine. (d) `FrozenInstanceError` is imported from `dataclasses` for the frozen-check assertion rather than catching bare `Exception` or `AttributeError`. **Files changed (2 new + 1 edited + docs):** `src/chronoagent/observability/components.py` (new, 41 lines), `src/chronoagent/main.py` (edited: +125 lines net, added 4 builder helpers + component_status wiring in lifespan), `tests/unit/test_graceful_degradation.py` (new, 370 lines, 22 tests), `PLAN.md` (9.3 Findings/Challenges/Decisions added, 9.3 ticked, Completed row updated), `CLAUDE.md` (this table). No new dependencies; `sqlalchemy.pool.StaticPool` and `sqlalchemy.text` are already in the environment. **Previous session carry-over:** Phase 9 task 9.2 merged to `main` via PR #24 squash commit `e3e4114`. Both CI runs (3.11 + 3.12) green on both workflow triggers; no test flakes reproduced on CI. Local main synced, remote + local feature branches deleted.** Local pre-merge run: ruff check + format --check clean on 106 files, mypy --strict clean on 69 source files, pytest 989 passed (963 prior + 26 new) 94.78% coverage, `src/chronoagent/retry.py` 100% covered. Two pre-existing ChromaDB cross-test-pollution flakes (`test_agents.py::TestSummarizerAgentSynthesize::test_synthesize_retrieves_templates` and one from `TestQuarantine`) reproduced ONLY under the coverage plugin and passed every time in isolation; same flakes documented from Phase 5.7/8.3, not introduced by this task. New module: `src/chronoagent/retry.py` exports four tenacity policies -- `llm_retry` (retry on `httpx.HTTPError`), `redis_retry` (retry on `redis.exceptions.RedisError`), `chroma_retry` (retry on `chromadb.errors.ChromaError`), `db_retry` (retry on `sqlalchemy.exc.OperationalError`, NOT `IntegrityError`). Each policy pinned at `stop_after_attempt(3)`, `wait_exponential(multiplier=0.25, min=0.25, max=2.0)`, `reraise=True`, and shares a `before_sleep_log(logger=chronoagent.retry, level=WARNING)` hook so retry storms show up under one logger name. **LLM backends:** `@llm_retry` on `TogetherAIBackend.generate` / `.embed` and `OllamaBackend.generate` / `.embed`; MockBackend untouched. **Redis:** `RedisBus.subscribe`/`unsubscribe` mutate `self._handlers` under a `threading.Lock` BEFORE the Redis round-trip, so wrapping the outer method would re-append the handler per retry. Extracted `_publish_raw` / `_pubsub_subscribe` / `_pubsub_unsubscribe` private methods and decorated those; the outer `subscribe`/`unsubscribe` now pulls the "should I call pubsub?" decision out of the critical section and invokes the raw helper outside the lock. **ChromaDB:** `MemoryStore` + `QuarantineStore` both grew private `_count_raw`/`_upsert_raw`/`_query_raw`/`_get_by_ids_raw`/`_get_all_raw`/`_delete_raw` (plus quarantine-specific `_get_ids_raw`/`_get_full_raw`/`_existing_ids_raw`) decorated with `@chroma_retry`. Public signatures unchanged. Note: `MemoryStore.add` re-embeds documents on retry if Chroma flakes -- but `LLMBackend.embed` already has its own `@llm_retry` and the Chroma retry only kicks in on `ChromaError`, so the re-embed cost is bounded. **Postgres (classes):** `AuditTrailLogger.log_event`, `EscalationHandler._persist_escalation` (extracted from `maybe_escalate` so only the DB insert retries, not the bus publish / audit log / cooldown state mutation), `EscalationHandler._recent_allocation_task_ids` -- decorated at the method level. **Postgres (FastAPI routers):** Module-level `@db_retry` helpers in each router own their own `with session_factory() as session:` block and are called by the route function. `escalation.py`: `_list_escalations_db`, `_apply_resolution` (write path, still raises `HTTPException` 404/409 from inside the retry scope -- HTTPException is not `OperationalError`, so it propagates on the first raise with no retry). `dashboard.py`: `_fetch_last_signal_rows`, `_fetch_timeline_rows`, `_fetch_allocation_rows`, `_fetch_escalation_rows`, `_fetch_pending_escalation_count`. `signals.py`: `_fetch_signal_rows(session, stmt)` -- takes the injected session rather than a factory because the route uses `Depends(get_db)`; accepts that retrying on a broken session may not recover. `metrics.py`: `_fetch_pending_count`. **NOT wrapped:** `monitor/collector.py:354 session.add` stays unwrapped because `persist_step`'s docstring says the session is caller-managed and `session.add` is an in-memory staging call, not a DB round-trip; retrying here is pointless and the caller's commit path owns the policy. **Test file:** `tests/unit/test_retry.py` with 26 tests. Parametrized `TestPolicyRetryBehaviour` covers all four policies with: succeed-on-third-attempt, succeed-on-second-attempt, exhaust-after-three-attempts, non-retryable-ValueError-propagates-first-raise, warning-logged-before-each-retry. Narrow checks: `TestDbRetryDoesNotRetryIntegrityError` (locks in the IntegrityError-is-a-bug decision), `TestLlmRetryCoversBothSubclasses` (ReadTimeout + HTTPStatusError), `TestRedisRetryCoversSubclasses` (TimeoutError). `TestPolicyDecoratorOnInstanceMethod` pins that tenacity decorators preserve `self` binding for both `llm_retry` and `db_retry`. Uses real `sqlalchemy.exc.OperationalError` / `IntegrityError` built from `DBAPIError`'s classmethod path (no engine needed) via a `_make_operational_error` / `_make_integrity_error` helper. Assertions on logging use `caplog.at_level(logging.WARNING, logger="chronoagent.retry")`. **pyproject.toml:** `tenacity>=8.0.0` added to `[project.dependencies]`. Already installed as a transitive dep of langchain (`tenacity 9.1.4` in the active env). **Files changed (9 product + 1 test):** `pyproject.toml`, `src/chronoagent/retry.py` (new), `src/chronoagent/agents/backends/together.py`, `src/chronoagent/agents/backends/ollama.py`, `src/chronoagent/messaging/redis_bus.py`, `src/chronoagent/memory/store.py`, `src/chronoagent/memory/quarantine.py`, `src/chronoagent/escalation/audit.py`, `src/chronoagent/escalation/escalation_manager.py`, `src/chronoagent/api/routers/escalation.py`, `src/chronoagent/api/routers/dashboard.py`, `src/chronoagent/api/routers/signals.py`, `src/chronoagent/api/routers/metrics.py`, `tests/unit/test_retry.py` (new). PLAN.md Phase 9 Log extended with 9.2 Findings / Challenges / Decisions / Completed 2026-04-11 and task 9.2 ticked. **Previous session carry-over: Phase 9 task 9.1.** New product file: `src/chronoagent/api/middleware.py` shipping `RateLimitConfig` (frozen dataclass, defaults `post_per_minute=10`, `get_per_minute=60`, `ws_max_concurrent=5`, `exempt_paths=("/health", "/metrics")`) and `RateLimitMiddleware` (pure ASGI, no `BaseHTTPMiddleware`, handles both HTTP and WebSocket scopes in a single `__call__`). HTTP path: per-client fixed-window counters keyed by `(client_ip, method, minute_bucket)` where `bucket = int(now // 60)`; on cap, responds 429 JSON `{"detail":"rate_limit_exceeded","limit":N,"retry_after_seconds":N}` with `Retry-After`, `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset` headers; allowed responses get `x-ratelimit-*` headers injected via a `send` wrapper on the first `http.response.start` message. WebSocket path: global concurrency counter; on cap, drains the initial `websocket.connect` then emits `websocket.close` code `1008` (policy violation) before the downstream handler is touched. Exempt paths bypass both HTTP and WS checks. Non-(GET\|POST) methods (PUT/DELETE/PATCH/OPTIONS) pass through untouched. Client IP is read from `scope["client"][0]` with a `"unknown"` shared-bucket fallback when missing or malformed (conservative, refuses to hand anonymous callers their own budget). `main.py` `create_app` grows two optional kwargs: `rate_limit_config: RateLimitConfig \| None = None` and `rate_limit_clock: Callable[[], float] \| None = None`, and installs the middleware via `app.add_middleware(RateLimitMiddleware, config=..., clock=...)` before any router. **Key gotchas locked in:** (1) Starlette's `TestClient` spawns a fresh event loop per concurrent WebSocket portal; an `asyncio.Lock` created lazily on one loop cannot be awaited from another. Use `threading.Lock` instead; critical sections are counter-increment only (no I/O) so it is fast enough for both production single-loop deployments and the multi-loop test harness. (2) WebSocket rejection MUST `await receive()` the initial `websocket.connect` before sending `websocket.close`, otherwise Starlette treats the close as a protocol violation and TestClient hangs. (3) On allowed HTTP requests we cannot hold the counter lock across `await self.app(...)` since the downstream app runs for arbitrary time; the lock is released as soon as the counter is incremented, and the `send` wrapper does its header injection without reacquiring. (4) `asyncio_mode = "auto"` is set in `pyproject.toml`, so `async def test_*` methods run without `@pytest.mark.asyncio`; the marker is not needed and no other test uses it. (5) Default budgets of POST=10/min and GET=60/min per client are far above what any single test function hits (each test gets a fresh `create_app()` -> fresh middleware -> fresh counter), so no existing test had to be adjusted. `test_pipeline_e2e.py` has a module-scoped fixture but it returns a `ReviewPipeline`, not a `TestClient`, so it never crosses middleware. **Test file:** `tests/unit/test_rate_limit_middleware.py` with 36 tests across `TestRateLimitConfigDefaults` (4 defaults locked), `TestHttpGetBudget`, `TestHttpPostBudget`, `TestPostAndGetBucketsAreIndependent`, `TestExemptPaths` (including `/metrics/sub` prefix match and `/healthcheck` non-match), `TestBucketRollover` (including a direct `_prune_http` test proving previous bucket survives and stale buckets are dropped), `TestNonGetPostMethodsPassThrough`, `TestPerClientIsolation` (one async test driving the middleware directly with crafted ASGI scopes so we can exercise distinct client IPs without spoofing through TestClient), `TestWebSocketConcurrency` (under-cap, over-cap close-1008, slot-freed-on-disconnect, exempt-ws-bypass), `TestClientIpExtraction` (5 edge cases for `_client_ip`: tuple, list, missing, empty, non-string host), `TestSecondsUntilNextBucket` (midway/boundary/clamp), and `TestCreateAppWiring` (proves real `create_app` installs the middleware, `/health` exempt under load, custom clock kwarg drives window rollover against `/api/v1/agents/health`). **Test + CI status at end of 9.1:** 963 tests pass (927 pre-9.1 + 36 new), 94.94% full-suite coverage, `src/chronoagent/api/middleware.py` 100% covered (111/111), ruff check + format --check clean on 104 src/ tests/ files, mypy --strict clean on 68 source files. PLAN.md Phase 9 Log filled in for 9.1 (Findings / Challenges / Decisions / Completed 2026-04-11) and 9.1 task box ticked. CLAUDE.md phase tracker still shows `[ ]` for Phase 9 overall, 9.2 through 9.6 remain. **Previous session carry-over: Phase 8 closed.** Task 8.3 shipped and merged to `main` as PR #21 squash commit `94570e7`; both CI runs (3.11 + 3.12) green before merge. Remote + local `feat/phase8-task8.3-prometheus-metrics` branches deleted; stale `origin/feat/phase8-task8.2-*` and `origin/feat/phase8-task8.3-*` tracking refs pruned via `git fetch --prune origin`. Next session starts on Phase 9.1 (rate limiting middleware) from a fresh branch cut off `main`. **What 8.3 shipped (now on main):** 4 new product files and 1 new test file, 1939 insertions, 14 deletions. New source: `src/chronoagent/observability/metrics.py` (`ChronoAgentMetrics` class with isolated `CollectorRegistry`, 8 gauges, 7 counters, 3 histograms, passive update methods plus `render()` returning the exposition payload), `src/chronoagent/observability/metrics_wiring.py` (`subscribe_metrics_to_bus`/`unsubscribe_metrics_from_bus` attach closures on `health_updates`, `escalations`, `memory.quarantine`; closures parse dict-or-dataclass payloads, tolerate malformed/non-dict payloads, drop silently with a warning), `src/chronoagent/api/routers/metrics.py` (`GET /metrics` with `include_in_schema=False`; calls `_refresh_poll_gauges(request, metrics)` to snapshot `system_health`, `pending_escalations`, `quarantine_size`, `memory_baseline_size`, `memory_pending_refit` from `app.state` before `metrics.render()`; 503 when sink missing; tolerates partial state). `src/chronoagent/main.py` wires the sink into the lifespan after the escalation handler (`app.state.metrics = ChronoAgentMetrics()` then `app.state.metrics_subscribers = subscribe_metrics_to_bus(bus, app.state.metrics)`; teardown mirrors). `create_app` includes the new `metrics_router`. **Metric taxonomy:** per-agent gauges `chronoagent_agent_{health,bocpd_score,chronos_score}` labelled by `agent_id`; aggregate gauges `chronoagent_system_health`, `chronoagent_escalation_queue_pending`, `chronoagent_memory_{quarantine_size,baseline_size,pending_refit}`; counters `chronoagent_health_updates` (label `agent_id`), `chronoagent_allocations` (labels `task_type`, `outcome` in `{assigned,escalated,fallback}`), `chronoagent_escalations` (label `trigger`), `chronoagent_quarantine_events`, `chronoagent_quarantined_docs`, `chronoagent_reviews` (label `risk`), `chronoagent_integrity_checks` (label `flagged`); histograms `chronoagent_allocation_bid_score` (label `task_type`, score buckets), `chronoagent_review_duration_seconds` (label `risk`, latency buckets), `chronoagent_integrity_aggregate_score` (score buckets). **Grafana:** `docs/grafana/chronoagent_dashboard.json`, 9 panels, templated `${DS_PROMETHEUS}` datasource, spans system health, queues, per-agent timeseries, allocation rates by task type/outcome, escalation rates by trigger, review-duration p50/p95 by risk, integrity-score p50/p95/p99. **Test file:** `tests/unit/test_metrics.py` with 38 tests covering sink isolation, each observer method, wiring fan-out, malformed-payload drops, router happy path, 503 when sink missing, partial-state tolerance, and the empty-scorer system-health default. **Key gotchas locked in:** (a) `prometheus_client` auto-appends `_total` to counter names so test assertions must query `chronoagent_foo_total`, and unset bare counters still emit a `0.0` sample (check `== 0.0`, not `is None`); unset labelled counters/histograms emit no sample (check `is None`). (b) `TestClient(create_app())` runs the real lifespan which uses the default SQLAlchemy pool; in-memory SQLite then gives each new connection a fresh empty database, so the `/metrics` handler's count query errors with "no such table: escalation_records". Fix: override `app.state.session_factory` with a `StaticPool`-backed engine after entering the `TestClient` context (same pattern as `test_dashboard_router.client_app`). (c) `TemporalHealthScorer` subscribes to `SIGNAL_CHANNEL`/`signal_updates` and publishes to `HEALTH_CHANNEL`/`health_updates`; publishing on the health channel does NOT populate the scorer's internal `_health_cache`. The metrics sink's per-agent gauges ARE updated via `metrics_wiring` subscribing on the health channel (direct sink update, not scorer cache), but `system_health` (poll-derived from the scorer cache) stays at its 1.0 default until something publishes on `signal_updates`. **Test + CI status:** 927 tests pass (889 pre-8.3 + 38 new), 94.81% coverage, ruff check + format --check clean on `src/ tests/`, mypy --strict clean on 67 files. Flake: `TestQuarantine` tripped once during the first coverage run with an unrelated "test_reason_recorded_when_provided" AssertionError but did NOT reproduce on the immediate rerun or with `--no-cov`; passed in isolation both times; not introduced by task 8.3. PLAN.md Phase 8 Log filled in (Findings / Challenges / Decisions / Completed 2026-04-11) and task 8.3 ticked. CLAUDE.md phase tracker flipped Phase 8 to `[x]`. README.md Phase 8 row ticked, badges bumped to 927 tests / 94% coverage, Currently line rewritten to describe the two observability fronts (human dashboard + Prometheus scrape) and flag Phase 9 as next. PR #21 merged via squash with `--delete-branch`; no stacked children to worry about this time. **Previous session carry-over:** Phase 8 tasks 8.1 + 8.2 merged to `main`. 8.1 (backend) landed as PR #18 squash commit `b1117a2`; 8.2 (frontend) landed as PR #20 squash commit `9071638`. Only 8.3 (Prometheus metrics + Grafana JSON) remains before the phase closes. Fresh branch `feat/phase8-task8.3-prometheus-metrics` already cut from main and checked out, ready for the next session. **PR workflow gotcha (lock this in):** stacked PRs against the original CLI flow *do not* auto-retarget when the base branch is deleted via `gh pr merge --delete-branch`. GitHub auto-closes them instead and refuses reopen because the base branch no longer exists. The first 8.2 PR was #19, stacked on `feat/phase8-task8.1-dashboard-backend`; when #18 was merged with `--delete-branch`, #19 was force-closed. Fix was to force-push the rebased 8.2 branch (dropped the two 8.1 commits via `git rebase --onto main ba00706`) and open a fresh PR #20 against main with the same description. Lesson: either do NOT pass `--delete-branch` on the base merge until stacked children are retargeted, or don't stack in the first place when the base is about to merge immediately. **What 8.2 shipped (now on main):** Single self-contained HTML at `src/chronoagent/dashboard/static/index.html` (~550 lines, inlined CSS + vanilla JS, Chart.js 4.4.1 from jsdelivr CDN, no build step, no npm). New `src/chronoagent/dashboard/__init__.py` exposes `STATIC_DIR` and `INDEX_HTML` path constants. `GET /dashboard/` added to `src/chronoagent/api/routers/dashboard.py` returning `FileResponse(INDEX_HTML, media_type="text/html")`, `include_in_schema=False`, 500 if the asset is missing. No `StaticFiles` mount; hatchling with `packages = ["src/chronoagent"]` bundles the HTML automatically, no pyproject changes needed. Layout is a 3x2 CSS grid (Agents left, Signal Explorer center, Allocation Log right; Memory Inspector spans 2 cols across the bottom, Escalation Queue bottom right). Dark theme with accent `#5aa9ff`, health thresholds at 0.75 / 0.5. WebSocket `/dashboard/ws/live` drives the header strip + agents list every 2s with 3s auto-reconnect; REST polls fill the rest (allocations + escalations 5s, memory 10s, timeline 10s or on click). Live frames omit `last_signal_at` for speed, so the client re-injects the last-known value from the prior render. XSS-safe via a local `escapeHtml` helper. `TestDashboardIndex` class added to `tests/unit/test_dashboard_router.py` (landmark-string + bundled-asset presence checks). **Test + CI status at merge:** 889 tests pass, 94.58% coverage, ruff check + format --check clean on `src/ tests/`, mypy --strict clean on 64 files, both PRs green on 3.11 + 3.12. PLAN.md ticked 8.2. CLAUDE.md phase tracker still shows `[ ]` for Phase 8 overall (8.3 outstanding) but 8.1 + 8.2 are done. README "Currently:" updated mid-phase; full phase-close README tick comes after 8.3. |

> Update this table at the end of every session before closing.

---

## How to Start a Session

```
1. Read this file (CLAUDE.md). You now have full context
2. Read only the current phase section from PLAN.md
3. Pick the next unchecked [ ] task
4. Implement → write test → make test green → [x]
5. Update "Current Status" table above before ending session
```

---

## Git Workflow

```
main          protected, never commit directly
feat/*        new features (one per phase task is fine)
fix/*         bug fixes
chore/*       config, CI, docs, refactor
```

**Branch per task:**
```bash
git checkout -b feat/phase0-pyproject    # start task
# ... implement + test ...
git add <specific files>                 # never git add .
cz commit                                # conventional commit prompt
git push origin feat/phase0-pyproject
gh pr create --base main                 # open PR
# CI must be green before merge
```

**Commit format (enforced by commitizen):**
```
feat(scope): short description
fix(scorer): handle zero-variance KL-div
chore(ci): add mypy strict step
docs(readme): add architecture diagram
test(allocator): add negotiation invariant tests
```

**Never:**
- `git add .` or `git add -A`, always add specific files
- Commit directly to `main`
- Skip pre-commit hooks (`--no-verify`)
- Commit `.env` files

---

## Dev Practices (enforced every task)

| Practice | Tool | When |
|----------|------|------|
| Lint + format | `ruff` | pre-commit + CI |
| Type checking | `mypy --strict` | pre-commit + CI |
| Commit message | `commitizen` | pre-commit |
| Test coverage | `pytest --cov` | CI (min 80%) |
| Dependency lock | `uv` + `requirements.lock` | CI installs from lock |
| Type hints | manual | every new function |
| Docstrings | manual | every public function |

**Test rule:** Write the test in the same session as the implementation, before moving to the next task. `make test` green = task done.

**Phase 1 exception:** Research experiment, no unit tests. Gate = measured signal results plus GO/NO-GO decision.

**Prose style rule (project-wide):** Never write em dashes (Unicode U+2014) in any file: README, docs, PLAN.md, CLAUDE.md additions, code comments, docstrings, commit messages. Use commas, colons, parentheses, semicolons, or two sentences instead. The hyphen `-` and en dash `–` are fine.

**README update rule:** Update the "Project status" section of `README.md` at the close of every phase. Tick the phase, update the test count and coverage, and add a one-line "Currently:" note. The README, `CLAUDE.md`, and `PLAN.md` stay in sync at session end.

---

## Stack Quick-Ref

| Component | Choice | Note |
|-----------|--------|------|
| LLM (real) | Together.ai | Default. Get key at api.together.ai |
| LLM (tests/experiments) | MockBackend | Zero cost, deterministic |
| LLM (optional) | Ollama | GPU only, skip |
| Forecaster | Chronos-2-Small + BOCPD | CPU-safe. Async. |
| Agents | LangGraph + LangChain | |
| Memory | ChromaDB | |
| API | FastAPI | |
| Messaging | Redis pub/sub | |
| DB | SQLite (dev) / PostgreSQL (prod) | |
| Dashboard | FastAPI + HTML/Chart.js | No npm build step |
| Container | Docker + Compose | |

---

## Key Commands

```bash
make dev              # start FastAPI with hot reload
make test             # pytest unit + integration (MockBackend, no API calls)
make lint             # ruff + mypy
make docker-up        # full stack
cz commit             # conventional commit
cz bump               # bump version + changelog
pre-commit run --all  # run all hooks manually
chronoagent run-experiment --config configs/experiments/signal_validation.yaml --output results/
```

---

## Phase Tracker (mirror of PLAN.md, update both)

| # | Phase | Status |
|---|-------|--------|
| 0 | Bootstrap | `[x]` |
| 1 | Signal Validation (GO/NO-GO) | `[x]` |
| 2 | Core Agent Pipeline | `[x]` |
| 3 | Behavioral Monitor | `[x]` |
| 4 | Temporal Health Scorer | `[x]` |
| 5 | Decentralized Task Allocator | `[x]` |
| 6 | Memory Integrity Module | `[x]` |
| 7 | Human Escalation Layer | `[x]` |
| 8 | Observability Dashboard | `[x]` |
| 9 | Production Hardening | `[x]` |
| 10 | Research Experiment Suite | `[ ]` |
| 11 | Paper Scaffold + Reproducibility | `[ ]` |
| 12 | CI/CD + Release | `[ ]` |

---

## Pivot Status

| Pivot | Triggered? | Action Taken |
|-------|-----------|--------------|
| A (AWT=0) | **Yes** | Concurrent detection reframe. No code changes. |
| B (no signal) | No | KL-div confirmed. 3/6 signals MockBackend constants. |
| C (Chronos underperforms) | No | n/a |

---

## Project Files

| File | Purpose |
|------|---------|
| `CLAUDE.md` | This file: session context, git workflow, quick-ref |
| `PLAN.md` | Full implementation plan: phases, tasks, logs, pivot protocol |
| `chrono_agent_research.md` | Research dossier: role alignments, literature gaps (for applications) |
| `docs/phase1_decision.md` | Phase 1 GO/NO-GO ruling, raw results, pivot analysis |
