# Production overrides — sensitive values come from env vars, not this file.
env: prod
llm_backend: together
database_url: postgresql+asyncpg://chrono:changeme@postgres:5432/chronoagent
redis_url: redis://redis:6379/0
chroma_persist_dir: /data/chroma
forecaster_enabled: true
